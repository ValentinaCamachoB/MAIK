spring:
  application:
    name: football

  datasource:
    url: jdbc:mysql://localhost:3306/footballdb
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   
    show-sql: true

server:
  port: 1234